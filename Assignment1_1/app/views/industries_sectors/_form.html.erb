<%= form_with(model: industries_sector) do |form| %>
  <% if industries_sector.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(industries_sector.errors.count, "error") %> prohibited this industries_sector from being saved:</h2>

      <ul>
        <% industries_sector.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
